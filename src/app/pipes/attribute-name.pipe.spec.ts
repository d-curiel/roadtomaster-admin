import { AttributeNamePipe } from './attribute-name.pipe';

describe('AttributeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
